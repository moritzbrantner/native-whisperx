# WhisperX CLI parity is the contract

native-whisperx targets Python WhisperX CLI feature parity as a current product
contract, beginning with WhisperX 3.8.6. The Rust implementation may differ
internally, but user-visible command behavior and outputs should be tracked
against that CLI baseline instead of treated as best-effort compatibility.
