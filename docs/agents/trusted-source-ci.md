# Trusted source CI ownership

Trusted source CI is implementation infrastructure. Its job is to prove the exact source graph for same-repository work; it does not publish, version, tag, or authorize releases. Registry-only verification remains a separate release concern.
