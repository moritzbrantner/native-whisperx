# Source-mode handoff

When feature work is verified against the exact source graph but registry-only CI is red only because the required upstream version has not been released yet, report the source-mode verification as the implementation result. Do not open or execute a release train unless the assigned work explicitly includes distribution.
