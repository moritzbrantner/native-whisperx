# Implementation vs release

Implementation validates the exact source graph. Release validates registry-only distribution. Do not turn the former into the latter merely because an upstream revision is unpublished.
