# Source-mode CI gate

For trusted same-repository feature work, the source-mode workflow is the dependency implementation gate. Registry CI remains useful compatibility evidence but cannot by itself require publication during feature work.
